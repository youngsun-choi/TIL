<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="resource.NewsMapper">
 	<select id="selectAllNews"  resultType="vo.NewsVO">
    	select id, writer, title, to_char(writedate,'yyyy-mm-dd') writedate, cnt from news order by id asc, writedate asc
 	</select>
 	<select id="selectOneNews" parameterType="_int"  resultType="vo.NewsVO">
    	select id, writer,title,content,cnt from news where id=#{id}
 	</select>
 	<select id="selectListWriter"  parameterType="string" resultType="vo.NewsVO">
    	select id, writer, title, to_char(writedate,'yyyy-mm-dd') writedate, cnt from news where writer = #{writer} order by id asc, writedate asc
    </select>
 	<select id="searchTitleNews"  parameterType="string" resultType="vo.NewsVO">
    	select id, writer, title, to_char(writedate,'yyyy-mm-dd') writedate, cnt from news where title like '%'||#{key}||'%' order by id asc, writedate asc
    </select>
    <select id="searchWriterNews"  parameterType="string" resultType="vo.NewsVO">
    	select id, writer, title, to_char(writedate,'yyyy-mm-dd') writedate, cnt from news where writer like '%'||#{key}||'%' order by id asc, writedate asc
    </select>
  	<insert id="insertNews"  parameterType="vo.NewsVO">
  		<selectKey resultType="int" keyProperty="id" order="BEFORE">
      		select news_seq.nextval from dual     
    	</selectKey>  
    		insert into news(id, writer, title, content, writedate, cnt) values(#{id},#{writer},#{title},#{content},sysdate,#{cnt})
	</insert>	
	<delete id="deleteNews"  parameterType="_int"  >
    	delete from news where id=#{id}
	</delete>
	<update id="updateNews"  parameterType="vo.NewsVO"  >
		update news set 
				writer=#{writer}, 
				title=#{title}, 
				content=#{content}, 
				cnt=#{cnt} +1
		where id=#{id}
	</update>
</mapper>